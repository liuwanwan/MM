<resources>

	<!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
		<!-- 启动时页面为透明色，避免显示空白页，好像有问题，还是空白页 -->
		<!--item name="android:windowIsTranslucent">true</item-->
		<!-- 这个起作用，启动时有点延时，实际就是加载了透明的背景，如微信6.3.28 -->
		<!--item name="android:windowBackground">@android:color/transparent</item-->
		<!-- 设置启动背景图片，避免显示空白页，如支付宝9.9.5 -->

	</style>
	<style name="SplashTeme" parent="AppTheme">
		<!-- 启动时页面为透明色，避免显示空白页，好像有问题，还是空白页 -->
		<item name="android:windowBackground">@drawable/splash_bg</item>
		<item name="android:windowFullscreen">true</item>
	</style>
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="MeiziDetailActivityTheme" parent="AppTheme">
    </style>
	<style name="MeiziTabLayout" parent="Widget.Design.TabLayout">
		<item name="tabIndicatorColor">?attr/colorAccent</item>   
		<item name="tabIndicatorHeight">2dp</item>    
		<item name="tabPaddingStart">8dp</item>    
		<item name="tabPaddingEnd">8dp</item>   
		<item name="tabBackground">?attr/selectableItemBackground</item>   
		<item name="tabTextAppearance">@style/MeiziTabTextAppearance</item>
		<item name="tabSelectedTextColor">#FFFFFF</item>
		</style>
	<style name="MeiziTabTextAppearance" parent="TextAppearance.Design.Tab">
		<item name="android:textSize">16sp</item>  
		<item name="android:textColor">?android:textColorSecondary</item>  
		<item name="textAllCaps">true</item>
	</style>
</resources>
